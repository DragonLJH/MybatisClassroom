<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="gupt.dragon.dao.ModalInforMapper">
	
	<select id="selectAllByDateAndMor" resultType="management" parameterType="management">
		select  date,mor,reserve,projectname,projectmanager,remark,classroom
			 from management where date = #{date} and mor = #{mor} and classroom = #{classroom}
	</select>

  
</mapper>